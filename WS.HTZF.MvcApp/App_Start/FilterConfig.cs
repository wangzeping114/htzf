using System.Web.Mvc;
using WS.HTZF.MvcApp.Filter;

namespace WS.HTZF.MvcApp.App_Start
{
    public class FilterConfig
    {
        public static void RegisterGlobalFilters(GlobalFilterCollection filter) 
        {
            
            //filter.Add(new ModelStateValidAttribute());
             //filter.Add(new ApplicationHandleErrorFilter());
           
        }
    }
}